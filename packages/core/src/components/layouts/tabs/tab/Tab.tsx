import React, {ReactNode} from "react";

import './Tab.css'

interface Props {
	children: ReactNode,
	uuid: string,
	name: string,
	icon?: string
}

export const Tab: React.FC<Props> = ({}) => {

	return (
		<></>
	)
}