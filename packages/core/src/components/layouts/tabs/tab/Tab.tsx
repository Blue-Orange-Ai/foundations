import React, {ReactNode} from "react";

import './Tab.css'

interface Props {
	uuid: string,
	name: string,
	icon?: string,
	children?: React.ReactNode
}

export const Tab: React.FC<Props> = ({}) => {

	return (
		<></>
	)
}